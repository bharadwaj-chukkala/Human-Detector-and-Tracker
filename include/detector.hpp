#include <iostream>
#include <vector>

// class detector {
//     std::array<int> drawboundingbox();
//     std::vector<point> applySVM();
//     public:
//     std::array<int> preprocess();
// }


class Detector{
    public:
    /**
     * @brief Preprocess the image, in this case we resize the image 
     * 
     * @param frame is the input frame 
     * @return cv::Mat is the resized/preprocessed image
     */
    cv::Mat preprocess(cv2::Mat frame) {

    };
}